basePath: /api/v1/2025
definitions:
  response.APIResponse:
    properties:
      code:
        type: integer
      data:
        description: data return null not show
      error:
        description: Error return null not show
      message:
        type: string
    type: object
host: localhost:8085
info:
  contact:
    email: nguyenphamhoangvu852004@gmail.com
    name: nguyenphamhoangvu
    url: https://github.com/nguyenphamhoangvu852004/ticket4u
  description: API Document for Event Ticket Service
  termsOfService: https://github.com/nguyenphamhoangvu852004/ticket4u
  title: API Document for Event Ticket Service
  version: "1.0"
paths:
  /events:
    get:
      consumes:
      - application/json
      description: Get list events with pagination, pass the JWT bearer token in the
        header to get the data from the organizer
      parameters:
      - default: "1"
        description: Page number
        in: query
        name: page
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/response.APIResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/response.APIResponse'
      security:
      - Bearer: []
      summary: Get list events
      tags:
      - Events
securityDefinitions:
  Bearer:
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
