services:
  eureka-server-v2:
    build:
      context: ../eureka-server
      dockerfile: Dockerfile
    container_name: eureka-server-v2
    ports:
      - "8761:8761"
    environment:
      - EUREKA_CLIENT_REGISTER_WITH_EUREKA=false
      - EUREKA_CLIENT_FETCH_REGISTRY=false
